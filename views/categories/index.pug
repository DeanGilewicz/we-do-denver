extends ../layout

block content
	.categories
		each category, i in categories
			div(class="category category--#{i}")
				.category__icon
					img(src="http://via.placeholder.com/350x200?text=icon" alt="")
				.category__name
					h2= Category
		

